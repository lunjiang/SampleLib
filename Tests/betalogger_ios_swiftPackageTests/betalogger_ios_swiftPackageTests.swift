import XCTest
@testable import betalogger_ios_swiftPackage

final class betalogger_ios_swiftPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(betalogger_ios_swiftPackage().text, "Hello, World!")
    }
}
